version: 2.1
jobs:
  test:
    docker:
      - image: irenecj/proyecto-idiomas
    steps:
      - checkout
      - run:
          name: Ejecutar test
          command: |
            grunt test
workflows:
  testeo:
    jobs:
      - test
